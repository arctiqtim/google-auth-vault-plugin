steps:
- name: 'gcr.io/cloud-builders/go'
  args: ['install', '.']
  env: ['PROJECT_ROOT=main']
- name: 'gcr.io/cloud-builders/go'
  args: ['build', 'google-auth-vault-plugin']
  env: ['PROJECT_ROOT=main']
artifacts:
  objects:
    location: 'gs://controlstation1-artifacts/'
    paths: ['google-auth-vault-plugin']